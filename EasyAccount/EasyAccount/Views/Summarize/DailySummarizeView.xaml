<UserControl
    x:Class="EasyAccount.Views.Summarize.DailySummarizeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasyAccount.Views.Summarize"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <StackPanel Width="123" Height="200">
            <TextBlock Text="{x:Bind summarize.weekName}" />
            <StackPanel Orientation="Horizontal">
                <TextBlock>
                收入：
                </TextBlock>
                <TextBlock Text="{x:Bind summarize.income}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock>
                支出：
                </TextBlock>
                <TextBlock Text="{x:Bind summarize.payout}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
